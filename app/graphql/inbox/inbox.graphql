# import Payment from '../generated/prisma.graphql'

extend type Query {
  payments: [PaymentEx]
}

type PaymentEx implements Node {
  id: ID!
  payment: Payment!
}

type Mutation {
  addPayment(id: ID!): Payment
}
