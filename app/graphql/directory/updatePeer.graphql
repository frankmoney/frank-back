# import Payment from '../generated/prisma.graphql'

extend type Mutation {
  updatePeer(
    peerId: ID!
    update: UpdatePeerUpdateInput!
  ): Peer!
}

input UpdatePeerUpdateInput {
  name: String!
}
