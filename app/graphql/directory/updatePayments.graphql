# import Payment from '../generated/prisma.graphql'

extend type Mutation {
  updatePayments(
    accountId: ID!,
    updates: [UpdatePaymentsUpdateInput!]!
  ): [Payment!]!
}

input UpdatePaymentsUpdateInput {
  paymentId: ID!
  categoryId: ID
}
