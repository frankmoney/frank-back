extend type Query {
  ledgerBarChart(
    accountId: ID!
    categoryId: ID
    dateMin: DateTime
    dateMax: DateTime
  ): LedgerBarChart!
}

type LedgerBarChart {
  items: [LedgerBarChartItem!]!
}

type LedgerBarChartItem {
  date: DateTime!
  income: Float!
  expenses: Float!
}
