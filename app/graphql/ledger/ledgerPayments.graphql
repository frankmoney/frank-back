# import Payment from '../generated/prisma.graphql'

extend type Query {
  ledgerPayments(
    accountId: ID!,
    first: Int!,
    after: ID,
    skip: Int,
    search: String,
    dateMin: DateTime,
    dateMax: DateTime,
    amountMin: Float,
    amountMax: Float,
    verified: Boolean,
    categoryId: ID
  ): [Payment!]!
}
