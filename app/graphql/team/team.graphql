# import Team from '../generated/prisma.graphql'

extend type Query {
  teams: [Team]
  team: TeamResult!
}

type TeamResult {
  ownProfile: Profile!
}

type Profile {
  id: String!
  email: String!
  lastName: String
  firstName: String
}
